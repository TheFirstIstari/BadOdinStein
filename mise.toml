[tools]
odin = "latest"

[tasks]
build = "odin build src/ -out:badodin -o:speed -lto:thin"
build-release = "odin build src/ -out:badodin -o:speed -lto:thin -define:ODIN_DEBUG=false"
clean = "rm -f badodin badodin-* *.mov *.mp4"
test = "./badodin --help"
